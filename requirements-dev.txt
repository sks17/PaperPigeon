# Local dev + test tooling. Install: python -m pip install -r requirements-dev.txt
-r requirements.txt
-r backend/repopulation/requirements.txt
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport
# No-Docker local Postgres+pgvector for Phase-1 dev (scripts/phase1_local_db.py). Dev-only.
pgserver>=0.1.4
