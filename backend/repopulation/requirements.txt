# Repopulation Engine runtime deps (separate from the legacy Flask app's requirements.txt).
SQLAlchemy>=2.0,<3.0
pgvector>=0.3.0
psycopg[binary]>=3.2
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
trafilatura>=1.8        # HTML -> main text for lab extraction (Phase 3)
