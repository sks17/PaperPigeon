"""SQLAlchemy models mirroring backend/repopulation/migrations/0001_initial.sql (schema `repop`).

Implemented by Cursor task P1-T01. The SQL migration is the source of truth; these models must
match its columns, types, and constraints exactly. The main thread wires the engine/session.
"""
